{
	"cmd" : ["export LD_LIBRARY_PATH=${LD_LIBRARY_PATH}:~/imebra && g++ -std=c++11 -Ilibrary/include ${file_name} -o ${file_base_name} -L. -limebra && ./${file_base_name} && rm -rf ${file_base_name}"],
	"selector" : "source.cpp",
	"working_dir" : "${file_path}",
	"shell" : true,
	"env" : {
				"LD_LIBRARY_PATH": "~/imebra"
			}
}